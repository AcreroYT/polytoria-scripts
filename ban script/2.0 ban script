-- UID goes here.
local banList = { 123, 456, 789 }

-- KILL
Players.PlayerAdded:Connect(function(plr)
	for i, v in banList do
		if plr.UserID == v then
			plr.Kick("You've been a bad boy!")
		end
	end
end)
